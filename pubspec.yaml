name: duration
description: Utilities to make working with 'Duration's easier. Formats duration in human readable form and also parses duration in human readable form to Dart's Duration.
version: 4.0.0
homepage: https://github.com/desktop-dart/duration

environment:
  sdk: '>=2.17.0 <4.0.0'

dev_dependencies:
  test: ^1.16.7
  lints: ^2.1.1
